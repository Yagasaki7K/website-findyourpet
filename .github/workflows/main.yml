name: Sync Fork v1.2.1

on:
  schedule:
    - cron: '* * * * *' # every 30 minutes
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.2.1
        with:
          github_token: ${{ secrets.SECRET_WORKFLOW }}
          owner: llvm
          base: main
          head: main
